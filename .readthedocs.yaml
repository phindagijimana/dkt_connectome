# Read the Docs configuration
# https://docs.readthedocs.io/en/stable/config-file/v2.html
# Project display name: DKT Connectome (see dwi_pipeline/mkdocs.yml site_name)

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

mkdocs:
  configuration: dwi_pipeline/mkdocs.yml

python:
  install:
    - requirements: dwi_pipeline/docs/requirements.txt
