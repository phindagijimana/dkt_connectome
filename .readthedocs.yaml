# Read the Docs configuration
# https://docs.readthedocs.io/en/stable/config-file/v2.html
# Project display name: DKT Connectome (see dwi_pipeline/docs/conf.py)

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: dwi_pipeline/docs/conf.py

python:
  install:
    - requirements: dwi_pipeline/docs/requirements.txt
